// find
// index
// indexOf
// inculdes

// 🥳
// Time Complexity : O(n)
const linearSearch = (arr, value) => {
  for (let i = 0; i < array.length; i++) {
    if (arr[i] === value) return i;
  }
  return -1;
};
